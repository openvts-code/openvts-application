import 'package:flutter/material.dart';

class SuperadminShell extends StatelessWidget {
  const SuperadminShell({required this.child, super.key});

  final Widget child;

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
